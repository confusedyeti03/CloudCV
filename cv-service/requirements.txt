# =============================================================================
# Python Dependencies for cv-service
# =============================================================================
# PURPOSE: Dependencies per FastAPI + WeasyPrint PDF generation
#
# WHAT TO DO:
# - Instal·lar: pip install -r requirements.txt
# - En producció, usar venv: python -m venv venv && source venv/bin/activate
#
# NOTE: WeasyPrint necessita dependencies del sistema
#       Ubuntu: apt install libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0
# =============================================================================

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# PDF generation (server-side fallback)
weasyprint>=68.0

# Templates
jinja2==3.1.3

# Data parsing
pyyaml==6.0.1

# File uploads (if needed)
python-multipart==0.0.6
