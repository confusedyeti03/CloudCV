# =============================================================================
# Role: deploy-landing - Handlers
# =============================================================================
---
# Landing page deployment handlers

- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
