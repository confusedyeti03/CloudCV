terraform {
  backend "s3" {
    # Backend blocks cannot use variables. Update these values manually.
    bucket       = "your-terraform-state-bucket"
    key          = "path/to/terraform.tfstate"
    region       = "your-aws-region"
    use_lockfile = true
    encrypt      = true
  }
}
